# Testing

Note: the tests won't run unless you first drag the files into the top level of the repository.

They also rely on the file paths being set in data_io.alum_cyl_file_paths - see the readme in data_io for more info.